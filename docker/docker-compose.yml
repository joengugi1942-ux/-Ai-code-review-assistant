version: "3.9"

services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - ../.env
    environment:
      - APP_ENV=production
    restart: unless-stopped
    volumes:
      - ../logs:/app/logs
